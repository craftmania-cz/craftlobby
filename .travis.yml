language: java
sudo: false # faster builds

script:
  - mvn package -Dmaven.javadoc.skip=true -V -B

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2/repository

after_success:
  - bash <(curl -s https://codecov.io/bash)